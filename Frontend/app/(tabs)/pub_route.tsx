import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";


export default function PubRoute() {
    return (
        <SafeAreaView>
            <Text>PubRoute</Text>
        </SafeAreaView>
    );
}